# FluxUI Documentation

FluxUI now follows a package-centric structure similar to `mcoli-ui`:

- docs are colocated under `packages/ui/content/docs`
- the UI package owns component and docs content together
- CLI entrypoints are available from `packages/cli/bin`

This keeps documentation and implementation closer, improves discoverability,
and makes package-level publishing workflows easier to maintain.
