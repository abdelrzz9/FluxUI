# Changelog

## 0.1.0

- Initial FluxUI CLI preview with `flutter_ui init`, `flutter_ui list`, and `flux add` commands.
- Added component copying, dependency installation, alias resolution, and typo suggestions for supported FluxUI components.
